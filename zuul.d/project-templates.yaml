- project-template:
    name: stx-api-ref-jobs
    description: |
      Build and publish api-ref document for StarlingX.
    check:
      jobs:
        - build-openstack-api-ref:
            files: &api-ref-triggers
              - ^os_api_ref/.*
              - ^api-ref/.*
              - ^doc/api_samples/.*
              - bindep.txt
              - doc/requirements.txt
              - test-requirements.txt
              - tox.ini
    gate:
      jobs:
        - build-openstack-api-ref:
            files: *api-ref-triggers
    promote:
      jobs:
        - promote-stx-api-ref:
            files: *api-ref-triggers

- project-template:
    name: publish-stx-docs
    description: |
      Runs and publishes documentation to StarlingX site.
    check:
      jobs:
        - openstack-tox-docs
    gate:
      jobs:
        - openstack-tox-docs
    promote:
      jobs:
        - promote-stx-tox-docs

- project-template:
    name: stx-release-notes-jobs
    description: |
      Runs the release notes test and publish jobs for StarlingX.
    check:
      jobs:
        - build-openstack-releasenotes
    gate:
      jobs:
        - build-openstack-releasenotes
    post:
      jobs:
        - publish-stx-releasenotes

- project-template:
    name: stx-lint-specfile-jobs
    description: |
      Lint RPM Specfiles for syntax and correctness
    check:
      jobs:
        - stx-check-specfile:
            voting: false

- project-template:
    name: stx-build-rpms
    description: |
      Deprecated no-op stub. The OBS/RPM build jobs
      (stx-obs-build-opensuse, stx-obs-build-centos) were removed, but this
      empty template is retained so existing consumers on r/stx.3.0
      (e.g. starlingx/config, starlingx/fault) continue to parse.
    check:
      jobs: []
